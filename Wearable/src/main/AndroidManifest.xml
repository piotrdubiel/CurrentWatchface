<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.watchface.current">
    <uses-feature android:name="android.hardware.type.watch" />

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".CalendarWatchface"
            android:label="@string/watchface_label_calendar"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:allowEmbedded="true">

            <meta-data android:name="com.google.android.clockwork.home.preview" android:resource="@drawable/preview_calendar" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.google.android.clockwork.home.category.HOME_BACKGROUND" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ColorfulWatchface"
            android:label="@string/watchface_label_colorful"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:allowEmbedded="true">

            <meta-data android:name="com.google.android.clockwork.home.preview" android:resource="@drawable/preview" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.google.android.clockwork.home.category.HOME_BACKGROUND" />
            </intent-filter>
        </activity>

    </application>

</manifest>
